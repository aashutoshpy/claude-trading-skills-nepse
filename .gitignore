# OS-generated files
.DS_Store
Thumbs.db

# Python cache & build artifacts
__pycache__/
.pytest_cache/
*.py[cod]
*.pyo
*.so
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Tool configurations
.idea/
.vscode/
.envrc
.mcp.json

# Logs & temp
logs/
*.log
*.tmp

# Jupyter
.ipynb_checkpoints/

# Generated reports
reports/
reviews/

# Per-symbol runtime state (e.g. parabolic-short SSR carryover files).
# Kept out of commits so production state never leaks into the repo.
state/

# CI artifacts
htmlcov/
.coverage
# .secrets.baseline is tracked — needed by detect-secrets pre-commit hook

# CSV OHLCV cache populated by `scripts/fetch_nepse_snapshot.py` — user data.
data/ohlcv/
data/indexes/
data/floorsheet/
data/constituents.csv

# Personal portfolio + cost basis — never commit. Templates are .example files.
data/meroshare_export.csv
data/nepse_portfolio.csv
data/cost_basis.csv

# Broker / mutual-fund transaction exports (contain BOID, name, and full
# transaction history). Drop any new file matching these patterns here.
data/*.xlsx
data/*.xls
data/*transaction*.csv
